# PRD: Card日消耗 — 操作列 & 导出明细

## 1. Summary

在现有「Card日消耗」列表能力（查询、分页、排序、导出数据）基础上，新增**操作列**与**导出明细**，支持对单日消耗查看/更新交易明细，以及按当前查询结果批量导出明细文件，满足运营对账与数据复核场景。

## 2. Background

相对截图基线能力，本需求补充两类增量功能：

1. **操作列**：支持单行「查看明细」「更新」
2. **导出明细**：在「导出数据」旁增加明细导出，导出范围跟随当前查询结果

## 3. Goals

- P0：可查看某日消耗对应的日交易明细
- P0：可导出当前查询列表对应的交易明细
- P0：更新、导出类操作需二次确认
- P1：更新成功后列表自动刷新，展示最新日消耗

## 4. Non-Goals

- 不改造查询条件与列表字段结构
- 不改变「导出数据」的导出内容范围（仍为日消耗汇总列表）
- 本期不包含批量勾选更新、明细行内编辑

## 5. Users and Use Cases

### Primary Users

- 资金 / 信用卡运营人员

### Key Use Cases

- 作为运营，我想查看某日消耗的交易明细，以便核对金额构成
- 作为运营，我想更新某日消耗数据，以便同步最新消耗结果
- 作为运营，我想导出当前筛选结果的交易明细，以便离线分析

---

## 6. Functional Requirements

### 6.1 操作列

| ID | Priority | Requirement | Acceptance Criteria |
| --- | --- | --- | --- |
| FR-OPS-001 | P0 | 列表最右侧增加「操作」列 | 每行展示操作入口；列名称为「操作」 |
| FR-OPS-002 | P0 | 提供「查看明细」 | 点击后弹窗展示该行对应的日交易明细 |
| FR-OPS-003 | P0 | 「查看明细」弹窗展示摘要 + 明细表 | 摘要含：账户别名、卡名/卡后4位、日期、日消耗；明细表含：交易时间、交易描述、交易金额、币种、交易状态、交易ID |
| FR-OPS-004 | P0 | 提供「更新」 | 点击后先弹出二次确认，确认文案简洁，不展示具体条数 |
| FR-OPS-005 | P0 | 确认更新后刷新界面 | 确认后更新该条日消耗及对应明细；列表刷新；提示「更新成功」 |
| FR-OPS-006 | P0 | 取消更新不改数据 | 二次确认点「取消」或关闭，不触发更新 |

**二次确认文案（更新）：**

- 标题：更新确认
- 正文：确认更新该条日消耗数据吗？
- 按钮：取消 / 确认更新

### 6.2 导出明细

| ID | Priority | Requirement | Acceptance Criteria |
| --- | --- | --- | --- |
| FR-EXP-001 | P0 | 在「导出数据」后增加「导出明细」按钮 | 按钮文案为「导出明细」；样式与「导出数据」同色系但有主次区分（实心主按钮 / 描边次按钮） |
| FR-EXP-002 | P0 | 导出范围 = 当前查询结果 | 仅导出当前筛选后的日消耗列表所关联的交易明细，不受分页限制（导全部筛选结果，非仅当前页） |
| FR-EXP-003 | P0 | 导出走二次确认 | 确认文案简洁，不展示具体条数 |
| FR-EXP-004 | P0 | 确认后导出文件 | 生成可下载文件（如 CSV）；成功提示「导出成功」 |
| FR-EXP-005 | P0 | 无数据时不可导出 | 当前查询无日消耗或无关联明细时，提示「暂无明细可导出」，不打开确认框 |
| FR-EXP-006 | P0 | 明细文件字段按固定顺序导出 | 见下方「导出明细字段说明」；表头与字段顺序必须一致 |

**二次确认文案（导出明细）：**

- 标题：导出确认
- 正文：确认导出当前查询结果的交易明细吗？
- 按钮：取消 / 确认导出

**导出明细字段说明：**

| 序号 | 字段名 | 数据来源 | 类型 | 格式 / 示例 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 1 | 账户别名 | 日消耗行 | 文本 | `Demo-account-alpha` | 所属卡账户别名 |
| 2 | Card ID | 日消耗行 | 文本 | `c_demoxxxxxxxx` | 卡片唯一标识 |
| 3 | 卡名 | 日消耗行 | 文本 | `Demo-HQ01` | 卡片名称 |
| 4 | 卡后4位 | 日消耗行 | 文本 | `2083` | 卡号后四位 |
| 5 | 日期 | 日消耗行 | 日期 | `YYYY-MM-DD`，如 `2026-07-26` | 日消耗归属日期 |
| 6 | 交易时间 | 交易明细行 | 日期时间 | `YYYY-MM-DD HH:MM:SS`，如 `2026-07-26 14:32:18` | 单笔交易发生/结算时间 |
| 7 | 交易描述 | 交易明细行 | 文本 | `DEMOADS *ABC123` | 商户/交易描述 |
| 8 | 交易金额 | 交易明细行 | 数值 | 保留 2 位小数，如 `128.50` | 单笔交易金额；不含币种符号 |
| 9 | 币种 | 交易明细行 | 文本 | `USD` | 交易币种 |
| 10 | 交易状态 | 交易明细行 | 枚举文本 | `成功` / `已撤销` | 单笔交易状态 |
| 11 | 交易ID | 交易明细行 | 文本 | `demo_tx_xxxxxxxxxxxx` | 交易唯一标识 |

**导出规则补充：**

1. 一行日消耗对应多行交易明细时，日消耗维度字段（账户别名～日期）在每条明细行重复写出
2. 字段顺序固定为上表序号 1～11，不可调整列顺序
3. 文件建议命名：`Card日消耗明细_YYYY-MM-DD.csv`（日期为导出当天）
4. 建议 UTF-8（带 BOM）编码，便于 Excel 正确打开中文表头

**与「导出数据」差异：**

| 能力 | 导出数据 | 导出明细 |
| --- | --- | --- |
| 导出对象 | 日消耗汇总列表 | 日消耗对应的交易明细 |
| 范围 | 当前查询结果 | 当前查询结果关联明细 |
| 二次确认 | 需要 | 需要 |
| 主要字段 | 账户别名、Card ID、卡名、卡后4位、日期、币种、日消耗 | 见上表 11 个字段 |

---

## 7. Interaction Notes

1. 操作列链接顺序建议：`查看明细`、`更新`
2. 「查看明细」为只读弹窗，提供关闭即可
3. 「更新」「导出明细」均需二次确认；确认弹窗提示区上下居中
4. 导出成功/更新成功使用轻量 Toast，不展示条数

## 8. QA Scenarios

1. 点击「查看明细」→ 弹窗字段与该行日消耗一致，明细金额合计与日消耗逻辑匹配（原型可按演示数据校验）
2. 点击「更新」→ 取消 → 列表数据不变
3. 点击「更新」→ 确认 → 日消耗金额变化且列表刷新，Toast「更新成功」
4. 有筛选条件时点「导出明细」→ 确认 → 仅包含筛选范围内记录的明细
5. 清空筛选且无数据时点「导出明细」→ 提示不可导出，无确认弹窗
6. 「导出数据」与「导出明细」按钮样式可区分，且均可完成二次确认导出

## 9. Open Assumptions

- 「更新」在业务侧对应重新拉取/同步该卡该日消耗；本原型以本地刷新模拟
- 导出明细默认导出**全部筛选结果**（跨分页），而非仅当前页
