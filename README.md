# CardBalance 原型

信用卡管理相关原型页面（Card 日消耗 / 额度 / 返点统计 / 返点明细）。

**Version:** `1.0.0`（见 `VERSION`）

## 本地预览

直接打开 `index.html`，或：

```bash
python3 -m http.server 8080
```

访问 http://localhost:8080

## GitHub Pages

推送到 `main` 后，在仓库 Settings → Pages 中选择 Deploy from branch：`main` / `/ (root)`。
