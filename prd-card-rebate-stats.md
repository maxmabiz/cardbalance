# PRD: Card返点统计

## 1. Summary

新增「Card返点统计」页面，用于按月查看 Card 返点汇总数据（返点比例、返点金额、消费金额、退款金额），支持月份区间查询、分页浏览，以及对单月数据下载明细。页面挂载于「信用卡管理」左侧菜单，供资金/运营人员核对月度返点结果。

## 2. Problem

- 现有能力缺少按月维度的返点汇总视图，难以快速核对月度返点与消费、退款关系
- 需要可筛选、可分页的数据列表，以及按月下载明细的入口

## 3. Goals

- P0：提供按月返点统计列表
- P0：支持统计月份起止查询与重置
- P0：支持分页浏览
- P0：支持单行下载，且需二次确认
- P1：与「Card返点明细」页面形成汇总 → 明细的能力互补（本期统计页不内嵌明细弹窗）

## 4. Non-Goals

- 不展示「业务线」查询条件与列表字段（当前仅广告业务线，无需区分）
- 本期不做多业务线对比、图表看板
- 本期不做批量下载、定时导出
- 不在本页直接编辑返点数据

## 5. Users and Use Cases

### Primary Users

- 资金 / 信用卡运营人员

### Key Use Cases

- 作为运营，我想按月份区间查询返点统计，以便核对月度返点结果
- 作为运营，我想分页浏览统计列表，以便快速定位目标月份
- 作为运营，我想下载某月返点明细，以便离线复核

---

## 6. Information Architecture

### 入口

- 一级模块：信用卡管理
- 菜单名称：Card返点统计
- 面包屑：`Fund / 信用卡管理 / Card返点统计`

### 页面结构

1. 页面标题区：标题「Card返点统计」+ 简要说明
2. 查询区：统计月份（起）、统计月份（止）、查询、重置
3. 列表区：数据表格 + 分页
4. 行操作：下载

---

## 7. Functional Requirements

### 7.1 页面与导航

| ID | Priority | Requirement | Acceptance Criteria |
| --- | --- | --- | --- |
| FR-NAV-001 | P0 | 左侧菜单新增「Card返点统计」 | 点击后进入该页面，面包屑同步更新 |
| FR-NAV-002 | P0 | 页面标题与说明 | 标题为「Card返点统计」；说明表达按月查询返点相关数据的用途 |

### 7.2 查询

| ID | Priority | Requirement | Acceptance Criteria |
| --- | --- | --- | --- |
| FR-QRY-001 | P0 | 支持「统计月份（起）」筛选 | 控件为月份选择；可选；按 `YYYY-MM` 过滤 |
| FR-QRY-002 | P0 | 支持「统计月份（止）」筛选 | 控件为月份选择；可选；按 `YYYY-MM` 过滤 |
| FR-QRY-003 | P0 | 点击「查询」按条件过滤列表 | 仅展示落在起止月份闭区间内的数据；查询后回到第 1 页 |
| FR-QRY-004 | P0 | 起止合法性校验 | 若起 > 止，提示「统计月份起不能晚于止」，不执行查询 |
| FR-QRY-005 | P0 | 点击「重置」清空查询条件 | 月份起、止均清空；列表恢复展示全部数据；回到第 1 页 |
| FR-QRY-006 | P0 | 不提供业务线筛选 | 页面无业务线查询项 |

### 7.3 列表展示

| ID | Priority | Requirement | Acceptance Criteria |
| --- | --- | --- | --- |
| FR-LIST-001 | P0 | 以数据表格展示统计结果 | 非卡片式月汇总；为标准列表 |
| FR-LIST-002 | P0 | 列表字段固定如下 | 见「列表字段」 |
| FR-LIST-003 | P0 | 金额类字段可读展示 | 返点金额 / 消费金额 / 退款金额带千分位与货币符号（如 `$x,xxx.xx`） |
| FR-LIST-004 | P0 | 返点比例突出展示 | 以百分比展示（如 `2.30%`） |
| FR-LIST-005 | P0 | 默认排序 | 默认按统计月份倒序（新月份在前） |
| FR-LIST-006 | P0 | 无数据态 | 无匹配数据时展示「暂无数据」 |
| FR-LIST-007 | P0 | 不展示业务线列 | 列表无业务线字段 |

**列表字段：**

| 序号 | 字段名 | 说明 | 示例 |
| --- | --- | --- | --- |
| 1 | 统计月份 | 统计所属年月 | `2026年7月` |
| 2 | 返点比例 | 当月返点比例 | `2.30%` |
| 3 | 返点金额 | 当月返点金额 | `$12,345.67` |
| 4 | 消费金额 | 当月消费金额 | `$800,000.00` |
| 5 | 退款金额 | 当月退款金额 | `$3,200.50` |
| 6 | 操作 | 行内操作 | `下载` |

### 7.4 分页

| ID | Priority | Requirement | Acceptance Criteria |
| --- | --- | --- | --- |
| FR-PAGE-001 | P0 | 支持分页 | 展示总条数、上一页/下一页、页码 |
| FR-PAGE-002 | P0 | 支持切换每页条数 | 可选 5 / 10 / 20 条每页；切换后回到第 1 页 |
| FR-PAGE-003 | P0 | 边界处理 | 首页禁用上一页；末页或无数据禁用下一页 |

### 7.5 下载

| ID | Priority | Requirement | Acceptance Criteria |
| --- | --- | --- | --- |
| FR-DL-001 | P0 | 每行提供「下载」 | 操作列展示「下载」入口 |
| FR-DL-002 | P0 | 下载前二次确认 | 确认文案简洁，不展示具体条数/月份数值细节以外的冗余信息 |
| FR-DL-003 | P0 | 确认后执行下载 | 提示「下载成功」（或等价成功反馈） |
| FR-DL-004 | P0 | 取消不触发下载 | 点取消或关闭弹窗，不下载 |

**二次确认文案（下载）：**

- 标题：下载确认
- 正文：确认下载该月返点明细吗？
- 按钮：取消 / 确认下载

---

## 8. Interaction Notes

1. 查询区按钮：`查询`（主按钮）、`重置`（次按钮）
2. 「重置」= 清空条件，不是恢复某组默认月份
3. 下载确认弹窗提示区上下居中，风格与系统其他二次确认一致
4. 本页「下载」针对**单月**；与「Card返点明细」的列表查询导出能力区分

## 9. QA Scenarios

1. 进入菜单「Card返点统计」→ 页面与面包屑正确
2. 不填月份直接查询 → 展示全部统计数据
3. 选择起止月份查询 → 仅展示区间内月份；总条数与分页正确
4. 起 > 止 → 提示错误，列表不变化
5. 重置 → 条件清空，列表恢复全部
6. 切换每页条数 / 翻页 → 数据与页码状态正确
7. 点击下载 → 取消 → 无成功提示、无下载发生
8. 点击下载 → 确认 → 成功反馈

## 10. Open Assumptions

- 统计维度为自然月；币种默认 USD
- 「下载」业务含义为下载该月返点相关明细文件；具体文件字段可与「Card返点明细」对齐，本期原型以成功反馈示意
- 当前仅单一业务线，故不展示业务线维度
